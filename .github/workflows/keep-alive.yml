name: Keep Streamlit app awake

on:
  schedule:
    - cron: "0 */8 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sSf https://select-fantasy-football-auction.streamlit.app/ -o /dev/null
